[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "PYTHONPATH=. alembic upgrade head && PYTHONPATH=. uvicorn main:app --host 0.0.0.0 --port $PORT"
